plugins {
    id 'java-library'
    id 'net.neoforged.gradle.userdev' version '7.0.170'
}

java {
    toolchain.languageVersion = JavaLanguageVersion.of(21)
}

base {
    archivesName = 'anvilcraft-dingdongji'
}

group = 'com.dingdongji'
version = '0.0.6'

repositories {
    mavenLocal()
    maven { url = 'https://maven.aliyun.com/repository/public/' }
    maven { url = 'https://maven.neoforged.net/releases' }
}

dependencies {
    implementation "net.neoforged:neoforge:${neo_version}"
    // 铁砧工艺（AnvilCraft）：编译期引用其锻造台/网络类
    implementation files('libs/anvilcraft-1.6.0-snapshot2127.jar')
}

tasks.withType(JavaCompile).configureEach {
    options.encoding = 'UTF-8'
    options.release = 21
}

// 跳过资源文件下载（编译JAR不需要纹理/音效）
tasks.matching { it.name.contains("DownloadAssets") }.configureEach {
    enabled = false
}
